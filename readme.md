# Rogue++

This is a simple roguelike game written in C++, with pdcurses library for UI. 

### Prerequisites
- MSYS2 with MinGW64 g++ compiler
- `pdcurses` library
- GNU Make

### Building
1. Clone this repository
2. Run `make` to build the game
3. Run `make run` to run the game